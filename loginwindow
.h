#ifndef Error in " 'loginwindow
.h '.toUpperCase().replace(/^[1-9]/, '_ ').replace(/[^_a-zA-Z1-9]/g, '_ ')": SyntaxError: Expected token `;'
#define Error in " 'loginwindow
    .h '.toUpperCase().replace(/^[1-9]/, '_ ').replace(/[^_a-zA-Z1-9]/g, '_ ')": SyntaxError: Expected token `;'

#include <QWidget>

    namespace Ui
{
    class LoginWindow;
}

class LoginWindow : public QWidget
{
    Q_OBJECT

public:
    explicit LoginWindow(QWidget *parent = nullptr);
    ~LoginWindow();

private:
    Ui::LoginWindow *ui;
};

#endif // Error in " 'loginwindow
.h '.toUpperCase().replace(/^[1-9]/, '_ ').replace(/[^_a-zA-Z1-9]/g, '_ ')": SyntaxError: Expected token `;'
